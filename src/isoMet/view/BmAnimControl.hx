package isoMet.view;
import createjs.easeljs.BitmapAnimation;

/**
 * ...
 * @author GuyF
 */
class BmAnimControl extends BitmapAnimation
{

	public function new() 
	{
		
		super();
	}
	
	public function tick (params) {
		/*
			var f = this._animation ? this._animation.frequency : 1;
			if (!this.paused && ((++this._advanceCount)+this.offset)%f == 0) {
					this.advance();
				}
			this.DisplayObject__tick(params);
			*/
	}

	
	
}